import React from 'react';
import Template from '../../Components/Template/Template';

type Props = {};

const TemplatePage: React.FC<Props> = ({}: Props): JSX.Element => {
  return (
    <div>
      <Template />
    </div>
  );
};

export default TemplatePage;
